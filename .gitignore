# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Project specific
/data/
*.pid
*.log
server.pid

# OS
.DS_Store
Thumbs.db

# AI session scaffolding (kept on disk, not in git)
/.ai/
/CLAUDE.md

# Local dev helpers (kept on disk, not in git)
/scripts/

# Secrets and credentials
.env.secrets
.env
.env.*
*.env
*.secret
credentials.json
secrets/

# Local config overrides
config.local.json
*.local.*

# rclone config (may contain tokens)
rclone.conf

# Local dev artifacts
TOKEN_BUDGET.md
.ai/TOKEN_BUDGET.md
config.json

# Local-only session scaffolding (kept on disk, not in git)
/BACKLOG.md
/REPO_MAP.md
/TODO.md

# Dispatcharr plugin hot-reload marker (runtime)
.reload_token
